# fixtures/

Populated once the Day-1 prerequisites are met (fixture selection, Figma/Unity access — see root README):

- `golden-elements.json` — hand-labeled Gate 1 yardstick (T1.3)
- `golden-matches.json` — hand-labeled Gate 2 yardstick (T2.1)
- `frame-export.png` — reference render of the chosen Figma frame

Both JSON files must validate against `packages/contracts/schemas/`.
